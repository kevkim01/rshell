#!/bin/bash

exit

exit && hello world || ls -a

exit || echo hi && echo sup

exit || ls -a 

echo hi || exit; ls

ls && exit || ls

ls || exit || ls

ls && exit && sl

ls && sl && exit

ls || sl || exit

sl || ls || exit

ls && ls && exit

exit && sl
